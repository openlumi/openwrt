. /lib/functions.sh

case "$(board_name)" in
	aqara,zhwg11lm |\
	xiaomi,dgnwg05lm )
    uci -q batch <<-EOF >/dev/null
set firewall.@zone[0].input='ACCEPT'
set firewall.@zone[1].input='ACCEPT'
EOF
    uci commit firewall
	;;
esac

exit 0
